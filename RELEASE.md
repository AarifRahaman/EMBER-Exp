# Release Notes

### 28 Apr 2025

**VERSION 0.4**

 * Renamed `mutant` to `saboteur` namespace
 * Removed `counter` saboteur

**VERSION 0.3**

 * Renamed `component` to `mutant` namespace
 * Reworked repository directory structure
 * Renamed library to `ember`
 * Added new logo and graphics to `doc` folder

### 20 Feb 2025

 * Added `netlist` example
 * Reworked port class

### 06 Dec 2024

 * Class `IComponent` renamed `IMutant`
 * Added counter to components library
 * Added POSIT support

### 03 Dec 2024

**VERSION 0.2**

 * Removed dependencies with legacy code (`IUnit`, `reg_t`, and `mem_t` removed)
 * Major refactor of the `IComponent` for higher fault model scalability
 * New Components library (added `ff`, and `dpsram` classes)
 * Added new memory dedicated example

### 28 Nov 2024

 * Updated the examples from legacy `IUnit` to `IModule` interface; 
 * Fault Library Update
 * Added Array Type